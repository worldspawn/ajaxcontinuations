<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="4.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">x86</Platform>
    <ProductVersion>8.0.30703</ProductVersion>
    <SchemaVersion>2.0</SchemaVersion>
    <ProjectGuid>{38DEF3E0-3DDC-4B30-A83B-4966481C9B95}</ProjectGuid>
    <OutputType>Library</OutputType>
    <AppDesignerFolder>Properties</AppDesignerFolder>
    <RootNamespace>ajaxcontinuations.js</RootNamespace>
    <AssemblyName>ajaxcontinuations.js</AssemblyName>
    <TargetFrameworkVersion>v4.0</TargetFrameworkVersion>
    <TargetFrameworkProfile>Client</TargetFrameworkProfile>
    <FileAlignment>512</FileAlignment>
    <SolutionDir Condition="$(SolutionDir) == '' Or $(SolutionDir) == '*Undefined*'">..\</SolutionDir>
    <RestorePackages>true</RestorePackages>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|x86' ">
    <PlatformTarget>x86</PlatformTarget>
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|x86' ">
    <PlatformTarget>x86</PlatformTarget>
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <OutputPath>bin\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  <PropertyGroup>
    <StartupObject />
  </PropertyGroup>
  <ItemGroup>
    <JsFiles Include="ajaxcontinuations.js" />
    <JsFiles Include="errorPolicy.js" />
    <JsFiles Include="navigatePolicy.js" />
    <JsFiles Include="resultPolicy.js" />
    <JsFiles Include="refreshPolicy.js" />
    <JsFiles Include="ajaxHelper.js" />
    <JsFiles Include="init.js" />
  </ItemGroup>
  <ItemGroup>
    <None Include="packages.config" />
  </ItemGroup>
  <Import Project="$(SolutionDir)\.nuget\nuget.targets" />
  <Target Name="Build">
    <MakeDir Directories="$(OutputPath)" Condition="!Exists($(OutputPath))" />
    <!--<ReadLinesFromFile File="%(JsFiles.Identity)">
      <Output TaskParameter="Lines" ItemName="lines" />
    </ReadLinesFromFile>
    <WriteLinesToFile File="$(OutputPath)ajax.continuations.js" Lines="@(Lines)" Overwrite="false" />-->
  </Target>
  <Target Name="Clean">
    <RemoveDir Condition="Exists($(OutputPath))" Directories="$(OutputPath)" />
  </Target>
  <Target Name="Rebuild" DependsOnTargets="Clean;Build">
  </Target>
  <Target Name="Merge" AfterTargets="Build">
    <Exec Command="copy &quot;$(ProjectDir)ajaxcontinuations.js&quot; + &quot;$(ProjectDir)errorPolicy.js&quot; + &quot;$(ProjectDir)navigatePolicy.js&quot; + &quot;$(ProjectDir)resultPolicy.js&quot; + &quot;$(ProjectDir)refreshPolicy.js&quot; + &quot;$(ProjectDir)ajaxHelper.js&quot; + &quot;$(ProjectDir)init.js&quot; &quot;$(OutputPath)jquery.ajaxcontinuations.js&quot;" />
    <!--<Exec Command="copy y| copy &quot;%(JsFiles.Identity)&quot; &quot;$(TargetDir)jquery.ajaxcontinuations.js&quot;" WorkingDirectory="$(ProjectDir)" />-->
  </Target>
</Project>